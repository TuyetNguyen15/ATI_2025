chạy Be : uvicorn main:app chạy fe: npm install => npm start

// Git cmd

git pull origin master (luôn pull code mới từ master về trước khi code thêm tính năng nào/ bẻ thêm nhánh mới/ push code lên master)
git checkout -b feature/view-chicken-page (tạo một nhánh tính năng mới để code)
git checkout *tên nhánh (lệnh dùng để đi tới một nhánh nào đó - để sửa code hoặc theo dõi)
git add . (dùng để add code mới để commit)
git commit -m "ghi commit thay đổi mói ở đây" (lệnh commit)
git push origin (dùng để push code lên chính nhánh đang ở)
git push origin master (dùng để push code lên nhánh chính master)